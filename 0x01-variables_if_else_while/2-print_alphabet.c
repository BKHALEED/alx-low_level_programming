#include <stdio.h>
/**
 * main - void
 * description: 'writing alphabets that will prnt small ltr alp'
 * Return: 0 (Success)
 */
int main(void)
{
	char ch;
	for (ch = 'a' ; ch <= 'z' ; ch++"\n")
	{
		putchar(ch);
	}
	return (0);
}
