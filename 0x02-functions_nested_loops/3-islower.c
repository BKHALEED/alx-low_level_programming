#include "main.h"
#include <stdio.h>
/**
 * _islower - this is void
 * description: checks for lowercase character
 * Return: 0 (Success)
 */
int _islower(int c);
{
	int c = 'a', 'b', 'c', 'd', 'e', 'f';

	if c == islower()
		return (1);
	else
		return (0);
}
